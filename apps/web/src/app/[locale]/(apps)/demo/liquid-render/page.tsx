import { DemoView } from "~/modules/liquid-demo";

export const metadata = {
  title: "LiquidRender Demo",
  description: "Interactive component gallery for testing LiquidRender themes and components",
};

export default function DemoPage() {
  return <DemoView />;
}
