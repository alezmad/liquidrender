# Context Library for WF-0009: Cognitive Context Framework
# Generated: 2025-12-27

metadata:
  workflow_id: WF-0009
  workflow_name: Cognitive Context Framework
  total_tokens: ~3500
  budget: 20000

# Core reference files
context_files:
  - path: CLAUDE.md
    type: reference
    purpose: Current project rules (to update)
    tokens: ~200

  - path: .context/CLAUDE.md
    type: reference
    purpose: Current context hub structure
    tokens: ~250

  - path: .claude/CONTEXT-FRAMEWORK-VISION.md
    type: reference
    purpose: Previous vision document (to supersede)
    tokens: ~3000

# Key insights from conversation (not files, but captured)
conversation_insights:
  - topic: "Cognitive Restoration"
    insight: "Context is externalized cognition, not information loading"

  - topic: "Wisdom Hierarchy"
    insight: "Wisdom > Knowledge > Information > Data"

  - topic: "LLM as Query Engine"
    insight: "Filesystem is storage, JSON is index, LLM is semantic layer"

  - topic: "Progressive Documents"
    insight: "Documents should be layered: identity → purpose → structure → TL;DRs → full"

  - topic: "Two Graphs"
    insight: "Concept graph (understanding) + Entity graph (implementation)"

# Files to create
deliverables:
  wave_0:
    - path: .claude/context/
      type: directory
    - path: .claude/context/schemas/entities.yaml
      type: schema
    - path: .claude/context/schemas/concepts.yaml
      type: schema
    - path: .claude/context/schemas/wisdom.yaml
      type: schema

  wave_1:
    - path: .claude/context/ORIENTATION.md
      type: orientation
      tokens: ~300
    - path: .claude/context/wisdom/how-to-create-component.md
      type: wisdom
      tokens: ~500
    - path: .claude/context/templates/progressive-doc.md
      type: template
      tokens: ~200

  wave_2:
    - path: .claude/context/COGNITIVE-CONTEXT-FRAMEWORK.md
      type: vision
      tokens: ~2500
