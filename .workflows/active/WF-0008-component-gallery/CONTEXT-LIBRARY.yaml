# Context Library for WF-0008 - Component Gallery
# Token budget: ~15,000 tokens

required:
  - path: packages/liquid-render/src/renderer/components/index.ts
    purpose: Component registry (liquidComponents map) - lists all 42 components
    tokens: ~2,500
    priority: critical

  - path: packages/liquid-render/dev/AIDemo.tsx
    purpose: Existing demo patterns, styling conventions
    tokens: ~4,800
    priority: high

  - path: packages/liquid-render/dev/ComponentTest.tsx
    purpose: Existing test data shapes
    tokens: ~1,200
    priority: high

  - path: packages/liquid-render/docs/COMPONENT-GUIDE.md
    purpose: Component authoring standards, token usage
    tokens: ~4,400
    priority: medium

reference_only:
  - path: packages/liquid-render/docs/COMPONENT-STATUS.md
    purpose: Component categories for sidebar navigation
    tokens: ~3,200
    priority: low
    note: Already discussed in conversation

  - path: packages/liquid-render/src/renderer/components/utils.ts
    purpose: Design tokens (colors, spacing, etc.)
    tokens: ~3,800
    priority: low
    note: Reference if needed for styling

already_loaded:
  - COMPONENT-GUIDE.md (read earlier)
  - COMPONENT-STATUS.md (read earlier)
  - AIDemo.tsx (read earlier)
  - ComponentTest.tsx (read earlier)
  - index.ts (read earlier)

total_estimated: ~8,500 tokens (required only)
budget_remaining: ~6,500 tokens

notes:
  - All key files already loaded in conversation
  - liquidComponents map has 42 entries (our target)
  - Faker data generators needed per component type
  - Gallery should match AIDemo styling conventions
